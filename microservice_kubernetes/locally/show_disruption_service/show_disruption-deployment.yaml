apiVersion: apps/v1
kind: Deployment
metadata:
  name: show-disruption-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: show-disruption
  template:
    metadata:
      labels:
        app: show-disruption
    spec:
      containers:
        - name: show-disruption
          image: show_disruption_service:latest
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "-c", "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"]
          ports:
            - containerPort: 8000